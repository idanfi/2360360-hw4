// todo: segmentation fault check
void main() {
   int x = 0;
   while(x < 1) {
       printi(x);
       x = x - 1;
       break;
       int y = 17;
       while (y > 8) {
            continue;
       }
   }
}